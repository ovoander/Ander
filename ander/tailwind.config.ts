import type { Config } from "tailwindcss";

export default {
    darkMode: "class",
    content: [],
    theme: {
        fontFamily: {
            sans: ["Nunito Sans", "sans-serif"],
        },
        extend: {},
    },
    plugins: [],
} satisfies Config;
